function [J, grad] = funcionCostoReg(theta, X, y, lambda)

m = length(y); 

% variables a retornar
J = 0;
grad = zeros(size(theta));

% ====================== CÓDIGO ======================







% =============================================================

end
