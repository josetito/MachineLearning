function [J, grad] = funcionCosto(theta, X, y)
%Calcula el costo y el gradiente para la regresión logística

% Inicializar
m = length(y);

% variables a retornar
J = 0;
grad = zeros(size(theta));

% ====================== CÓDIGO ======================
%
% Nota: grad debe tener la misma dimensión que theta
%








% =============================================================

end
